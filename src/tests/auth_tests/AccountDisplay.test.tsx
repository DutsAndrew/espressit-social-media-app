import React from 'react';
import { render, screen } from '@testing-library/react';
import "@testing-library/jest-dom";
import userEvent from "@testing-library/user-event";
import Header from '../../components/Header';
import { User } from "firebase/auth";

describe('unit test for account display', () => {
  test('drop down menu opens on account click', () => {
  
  });
  
  test('drop down menu closes on click away', () => {
  
  });
  
  test('click on drop down sign out, logs user out, displays login, and sign in again', () => {
  
  });
  
  test('click on drop down edit profile sends user to edit profile page', () => {
  
  });
});