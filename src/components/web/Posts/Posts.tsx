import React from "react";

const Posts = () => {

  // controller to use algorithms for sorting feed based on new, most popular, and posts that the user has contributed to

  return (
    <p>Controller for Posts</p>
  );
};

export default Posts;